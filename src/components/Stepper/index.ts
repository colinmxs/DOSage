/**
 * Stepper Component
 *
 * DOS-style step progress indicator / wizard component.
 */

export { createStepper } from './Stepper';
export type { StepperProps, StepperElement, Step, StepperOrientation } from './Stepper.types';
